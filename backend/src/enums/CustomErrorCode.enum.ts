export enum CustomErrorCode {
  ACCOUNT_LOCKED = 5000,
  IMAGE_NOT_FOUND = 3001,
  INVALID_FILE_TYPE = 2999,
  LIMIT_FILE_SIZE = 2998,
  PRODUCT_NOT_FOUND = 2000,
  DUPLICATE_PRODUCT = 2001,
  CART_NOT_FOUND = 3000,
  CART_EMPTY = 3002,
  EMAIL_CONFLICT = 4001,
  INCORRECT_PASSWORD = 4002,
  INVALID_REFRESH_TOKEN = 423,
  TOKEN_EXPIRED = 424,
}
